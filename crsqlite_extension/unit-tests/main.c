#include <stdio.h>
#include <assert.h>
#include <sqlite3.h>


/* main.c */
int main(int argc, char *argv[]) {
    // printf("\n========================Starting tests=======================\n");

    // sqlite3 *db;
    // int rc = sqlite3_open(":memory:", &db); 
    // if (rc != SQLITE_OK){
    //     return -1;
    // }

    // char *err_str;
    // rc = sqlite3_extension_init(db, &err_str, NULL);

    // test_init_augmentation_schemas();
    // test_augment_table_two_layer_callback();

    // printf("Tests ran successfully\n");

    return 0;
}