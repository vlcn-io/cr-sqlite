/**
 * Takes the results of an outbound stream and
 * ingests them into a database.
 *
 * Do you need this? Could you not just write `OutboundStream` to
 * `applyChanges`?
 */
export default class InboundStream {}
